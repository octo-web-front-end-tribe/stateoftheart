<div class="container">
    <div class="row">
    <h2>Ajouter projet</h2>
    <% if (!added) { %>
    <form method="POST">
        <div class="form-group">
            <label>Name</label>
            <input type="text" name="name" class="form-control">
        </div>
        <div class="form-group">
            <label>stack</label>
            <textarea name="stack" class="form-control" rows="3"></textarea>
        </div>
         <button type="submit" class="btn btn-primary">Ajouter</button>
    </form>
    <%} else {%>
    <div class="alert alert-success" role="alert">Project has been added !</div>
    <%}%>
    </div>
</div>